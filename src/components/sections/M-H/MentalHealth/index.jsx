import React, { useState } from "react"
import styles from "./styles.module.css"
import classNames from "classnames"


export default function MentalHealth({ errors, watch, register }) {
    return "mental health"
}
